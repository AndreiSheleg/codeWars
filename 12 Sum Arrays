// Sum Numbers
function sum (numbers) {
let result = 0;
 
  for (i = 0; i < numbers.length; i += 1) {
    result += numbers[i];
  };
    if (numbers.length === 0) {
    result = 0;
  };
  
  return result;    
    
};
